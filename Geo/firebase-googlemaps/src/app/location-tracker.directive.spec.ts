import { LocationTrackerDirective } from './location-tracker.directive';

describe('LocationTrackerDirective', () => {
  it('should create an instance', () => {
    const directive = new LocationTrackerDirective();
    expect(directive).toBeTruthy();
  });
});
